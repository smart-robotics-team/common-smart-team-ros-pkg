#!/usr/bin/env python
import roslib;roslib.load_manifest('common_float_multiplexer')

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

gen.add("starting_index", int_t, 0, "Autogenerated parameter based on model.", 0, -100, 100)

exit(gen.generate("common_float_multiplexer", "common_float_multiplexer", "common_float_multiplexer"))
